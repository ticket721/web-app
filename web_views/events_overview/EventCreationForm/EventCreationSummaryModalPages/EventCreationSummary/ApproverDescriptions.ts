
export const ApproverDisabled = (args: any, t: any): string => {
    const desc = t('ApproverDisabled_concise_description');

    return desc;
};

export const ApproverBlockOutside = (args: any, t: any): string => {
    const desc = t('ApproverBlockOutside_concise_description');

    return desc;
};
