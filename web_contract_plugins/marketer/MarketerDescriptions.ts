export const MarketerDisabled = (args: any, t: any): string => {
    const desc = t('MarketerDisabled_concise_description');

    return desc;
};

export const MarketerDirectSale = (args: any, t: any): string => {
    const desc = t('MarketerDirectSale_concise_description');

    return desc;
};
